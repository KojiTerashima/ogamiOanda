# ogamiOanda Copilot Instructions

Begin with the repository contract in [AGENTS.md](../AGENTS.md).

- Apply the path-specific files in [`instructions/`](instructions/) when they match the files being changed.
- Select a workflow from [`prompts/`](prompts/), [`agents/`](agents/), or [`skills/`](skills/) only when its purpose matches the request.
- Keep repository behavior and operational boundaries in `AGENTS.md` instead of duplicating them here.
