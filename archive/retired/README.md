# Retired assets

Historical snapshots only. Do not read, search, extract, import, or execute these bundles during ordinary agent work.
Use [the archive policy](../../docs/archive-policy.md) and `manifest.json` only for an explicitly needed historical investigation or restoration.

The manifest records original paths and SHA-256 hashes. Three legacy files contain redacted credential-like literals; their stored hashes intentionally differ from their original hashes.

`../classPositionForTest.py` is still an active compatibility dependency and is not part of these bundles.
